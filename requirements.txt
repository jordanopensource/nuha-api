# =============================================================================
# Nuha API - Python Dependencies
# =============================================================================

# Inference runs on ONNX Runtime (CPU). onnxruntime is a normal PyPI package, so
# no custom wheel index is needed, unlike the old torch +cpu build. transformers is
# kept ONLY for AutoTokenizer (the models are exported to ONNX and loaded via
# onnxruntime, not transformers' model classes). This file is the human-edited
# source of direct deps; requirements.lock is the generated, hashed lock.

# Web Framework
fastapi>=0.115.0,<1.0.0
pydantic>=2.0.0,<3.0.0
uvicorn[standard]>=0.32.0,<1.0.0

# Machine Learning
onnxruntime>=1.18.0,<2.0.0
transformers==5.3.0

# Text Processing
emoji>=2.0.0,<3.0.0
