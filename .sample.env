# =============================================================================
# EgyNuha API - Runtime Environment Configuration
# =============================================================================
# This file configures the API at RUNTIME (when the container starts).
# Run `samplr` to generate .sample.env from this file.
#
# NOTE: Build-time variables (HF_MODEL_REPO, HF_TOKEN) are NOT included here.
# Those are configured in the Dockerfile, not at runtime.
#
# DEFAULTS: All variables have sensible defaults in the Python code.
# You only need to set variables here if you want to override those defaults.
# =============================================================================


# -----------------------------------------------------------------------------
# Classification Engine
# -----------------------------------------------------------------------------

# Number of worker threads for concurrent classification requests
# Recommended: Set to number of CPU cores available
# Higher values improve throughput but increase memory usage
CLASSIFIER_WORKERS=4

# Maximum number of texts allowed in a single batch request
# Increase for higher throughput, decrease to limit memory usage per request
MAX_BATCH_SIZE=1000


# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------

# Logging verbosity level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log output format
# Options:
#   - "text": Human-readable format (recommended for development)
#   - "json": Structured JSON format (recommended for production/log aggregation)
LOG_FORMAT=text


# -----------------------------------------------------------------------------
# Server Configuration (Uvicorn)
# -----------------------------------------------------------------------------

# Host address to bind the server to
# Use "0.0.0.0" to accept connections from any interface
HOST=0.0.0.0

# Port number for the HTTP server
PORT=8000

# Number of Uvicorn worker processes
# Recommended: (2 x CPU cores) + 1 for production
# Note: Keep at 1 if using GPU inference to avoid memory issues
WORKERS=1

# Request timeout in seconds
# Increase if processing large batches
TIMEOUT=120


# -----------------------------------------------------------------------------
# Advanced Configuration (rarely needed)
# -----------------------------------------------------------------------------

# Path to the classification model
# Default in container: /home/appuser/model (set in Dockerfile)
# Default for local dev: ./model (set in Python code)
# Only override if mounting a custom model location
MODEL_PATH=


# -----------------------------------------------------------------------------
# CI/CD Secrets (Reference Only)
# -----------------------------------------------------------------------------
# These must be configured as secrets in Woodpecker CI, not in .env files.
#
# Build-time secrets:
#   HF_TOKEN             - (Optional) HuggingFace token for private/gated models
#
# CI secrets:
#   DOCKER_HUB_USERNAME  - Docker Hub username for pushing images
#   DOCKER_HUB_PASSWORD  - Docker Hub password or access token
#   SLACK_WEBHOOK        - Slack webhook URL for build notifications
#
# Build-time configuration (in Dockerfile, not here):
#   HF_MODEL_REPO        - HuggingFace model repository (default: SafwanLjd/egynuha-classifier)
